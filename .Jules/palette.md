## 2025-05-15 - Missing form associations and icon button ARIA labels in Admin UI
**Learning:** Many older forms and icon buttons lack proper `for`/`id` bindings and `aria-label`s, breaking screen reader navigation.
**Action:** Ensure all new form inputs have associated labels and icon-only buttons include descriptive ARIA labels.

version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./franklin_flyers.db:/app/franklin_flyers.db
    restart: always

volumes:
  db_data:

services:
  web:
    build: .
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./franklin_flyers.db:/app/franklin_flyers.db
    restart: always

volumes:
  db_data:
